# you can use str() or repr()

# using str()
cat = 978
C = str(cat)
print(C)


# using repr
# repr() is meant to generate representations which can be read by the interpreter
# (or will force a SyntaxError if there is no equivalent syntax).
quest = 123
K = repr(quest)
print(K)
